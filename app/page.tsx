import Link from 'next/link';
import {products,money} from '@/lib/demo';

export default function Home(){
  return <main><div className="shell"><nav className="nav"><div className="brand">Vanta<span>Cart</span></div><div className="navlinks"><span>Shop</span><span>Trending</span><span>Collections</span><span>Track order</span></div><div className="actions"><span className="demo">DEMO MODE</span><Link className="btn light" href="/admin">Admin</Link></div></nav><section className="hero"><div className="eyebrow">Curated global finds</div><h1>Products worth discovering.</h1><p>We surface standout products, price them intelligently and route fulfillment through a global supplier network — all from one storefront.</p><div className="buttons"><button className="btn" style={{background:'#d8ff76'}}>Shop trending</button><Link className="btn light" href="/admin">Open demo dashboard</Link></div></section><section className="kpis"><div className="kpi"><b>48h</b><span>Product testing cycle</span></div><div className="kpi"><b>40%+</b><span>Target gross margin</span></div><div className="kpi"><b>6–12d</b><span>Preferred delivery window</span></div><div className="kpi"><b>100%</b><span>Demo — no real orders</span></div></section><div className="sectionhead"><div><p>CURATED BY DATA</p><h2>Trending discoveries</h2></div><p>Opportunity score combines margin, demand, delivery and supplier signals.</p></div><section className="grid">{products.map(p=><Link key={p.slug} href={`/products/${p.slug}`} style={{textDecoration:'none',color:'inherit'}}><article className="card"><div className="visual"><span className="tag">{p.tag}</span>{p.emoji}</div><h3>{p.name}</h3><div className="meta">★ {p.rating} · {p.orders.toLocaleString()} orders · {p.category}</div><div className="price"><b>{money(p.price)}</b><span className="score">Score {p.score}</span></div></article></Link>)}</section><section className="strip"><div><b>Supplier-aware</b><span>Products are designed to map to one or more fulfillment sources.</span></div><div><b>Margin protected</b><span>Pricing rules can pause products before they become unprofitable.</span></div><div><b>Worldwide ready</b><span>Country rules and shipping profiles are first-class data.</span></div><div><b>Trackable</b><span>Order states are ready for supplier and carrier tracking events.</span></div></section><footer className="footer">VantaCart demo storefront · No live payments, supplier purchases or advertisements are enabled.</footer></div></main>
}
