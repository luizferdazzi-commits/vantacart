import Link from 'next/link';import '../editorial.css';import {entriesByKind,Lang} from '../../lib/editorial';
export const metadata={title:'Guias de software, IA e negócios',description:'Guias práticos para escolher software, IA, CRM, automação e ferramentas digitais.',alternates:{canonical:'/guides'}};
export default async function Page({searchParams}:{searchParams:Promise<{lang?:string}>}){const sp=await searchParams;const lang:Lang=sp.lang==='en'?'en':'pt';const pt=lang==='pt';return <main className="editorialIndex"><nav className="editorialNav"><Link href={`/?lang=${lang}`}>← VantaCart</Link></nav><div className="editorialWrap"><h1>{pt?'Guias':'Guides'}</h1><p>{pt?'Conteúdo prático para avaliar ferramentas antes de contratar.':'Practical content to evaluate tools before subscribing.'}</p><div className="editorialIndexGrid">{entriesByKind('guide').map(e=><Link className="editorialIndexCard" href={`/guides/${e.slug}?lang=${lang}`} key={e.slug}><span>{e.category}</span><h2>{e.title[lang]}</h2><p>{e.description[lang]}</p></Link>)}</div></div></main>}
