'use client';

import { useEffect, useMemo, useRef, useState } from 'react';
import { ArrowRight, CheckCircle2, ExternalLink, Search, SlidersHorizontal } from 'lucide-react';
import { trackEvent } from './Analytics';

type Lang='pt'|'en';
type Campaign={id:string;name:string;advertiser:string;description?:string;url?:string;status?:string;trackingLink?:string;allowsDeeplinking?:string;type?:string;network?:'impact'|'hotmart';category?:string};
type Category='all'|'ai'|'business'|'home'|'creators'|'productivity'|'technology'|'gaming';

const CACHE_KEY='vantacart_active_affiliate_campaigns_v3';
const editorialRank=['leadlovers','gearup for mobile','gearup','movavi','pixverse','creao','riibase','protoarc','vidu','gamsgo','appy pie','lorka','wizstar'];
const brandDomains:[RegExp,string][]=[
  [/leadlovers/i,'leadlovers.com'],[/protoarc/i,'protoarc.com'],[/movavi/i,'movavi.com'],[/pixverse/i,'pixverse.ai'],
  [/gearup/i,'gearupbooster.com'],[/creao/i,'creao.ai'],[/riibase/i,'riibase.com'],[/vidu/i,'vidu.com'],[/gamsgo/i,'gamsgo.com'],
  [/appy pie/i,'appypie.com'],[/lorka/i,'lorka.com'],[/wizstar/i,'wizstar.ai']
];
function slugify(value:string){return value.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');}
function landingUrl(c:Campaign,lang:Lang){return `/offers/${slugify(c.name)}?lang=${lang}`;}
function shortDescription(c:Campaign,lang:Lang){if(c.description)return c.description;return lang==='pt'?'Oferta ativa de parceiro afiliado verificado.':'Active offer from a verified affiliate partner.';}
function brandDomain(c:Campaign){const text=`${c.name} ${c.advertiser}`;return brandDomains.find(([rx])=>rx.test(text))?.[1];}
function brandLogo(c:Campaign){const domain=brandDomain(c);return domain?`https://www.google.com/s2/favicons?domain=${domain}&sz=128`:null;}
function quickBenefit(c:Campaign,lang:Lang){const s=`${c.name} ${c.advertiser} ${c.description||''}`.toLowerCase();if(/leadlovers/.test(s))return lang==='pt'?'Automação de marketing e vendas':'Marketing & sales automation';if(/protoarc/.test(s))return lang==='pt'?'Ergonomia para trabalho e home office':'Ergonomics for work & home office';if(/movavi/.test(s))return lang==='pt'?'Edição de vídeo simples e profissional':'Simple, professional video editing';if(/pixverse|vidu/.test(s))return lang==='pt'?'Criação de vídeos com IA':'AI video creation';if(/gearup/.test(s))return lang==='pt'?'Menos lag e melhor experiência em jogos':'Less lag, smoother gaming';if(/creao|appy pie/.test(s))return lang==='pt'?'Crie e automatize com IA':'Build and automate with AI';if(/riibase|crm/.test(s))return lang==='pt'?'Organize clientes e acelere vendas':'Organize customers and accelerate sales';if(/gamsgo/.test(s))return lang==='pt'?'Assinaturas digitais com economia':'Save on digital subscriptions';return lang==='pt'?'Parceiro selecionado pelo VantaCart':'VantaCart selected partner';}
function categoryOf(c:Campaign):Category{const explicit=(c.category||'').toLowerCase();if(['ai','business','home','creators','productivity','technology','gaming'].includes(explicit))return explicit as Category;const s=`${c.name} ${c.advertiser} ${c.description||''}`.toLowerCase();if(/gearup|game booster|gaming|games|ping|packet loss|lag/.test(s))return 'gaming';if(/protoarc|keyboard|mouse|ergonomic|workspace|desk|home office/.test(s))return 'home';if(/pixverse|vidu|wizstar|movavi|video|image|creator|creative|design|film|audio|multimedia/.test(s))return 'creators';if(/leadlovers|riibase|crm|sales|business|marketing|customer|accio|supplier|ecommerce|e-commerce/.test(s))return 'business';if(/gamsgo|productivity|workflow|organize|subscription/.test(s))return 'productivity';if(/creao|lorka|verdent|medo|appy pie|presentation intelligence|deepvinci|artificial intelligence|\bai\b|automation|agent|app builder/.test(s))return 'ai';return 'technology';}
function score(c:Campaign){const n=`${c.name} ${c.advertiser}`.toLowerCase();const idx=editorialRank.findIndex(k=>n.includes(k));return idx<0?0:100-idx*7;}
function categoryLabel(cat:Category,lang:Lang){const pt:{[K in Category]:string}={all:'Todas',ai:'IA & Software',business:'Negócios',home:'Home office',creators:'Criadores',productivity:'Produtividade',technology:'Tecnologia',gaming:'Gaming'};const en:{[K in Category]:string}={all:'All',ai:'AI & Software',business:'Business',home:'Home office',creators:'Creators',productivity:'Productivity',technology:'Technology',gaming:'Gaming'};return (lang==='pt'?pt:en)[cat];}
function OfferCard({campaign,index,lang}:{campaign:Campaign;index:number;lang:Lang}){const ref=useRef<HTMLAnchorElement|null>(null);useEffect(()=>{const el=ref.current;if(!el)return;let sent=false;const obs=new IntersectionObserver(entries=>{if(!sent&&entries.some(e=>e.isIntersecting&&e.intersectionRatio>=.45)){sent=true;trackEvent('view_offer',{offer_id:campaign.id,offer_name:campaign.name,partner:campaign.advertiser,network:campaign.network||'impact',category:categoryOf(campaign),language:lang,rank_position:index+1});obs.disconnect();}},{threshold:[.45]});obs.observe(el);return()=>obs.disconnect();},[campaign,index,lang]);const href=landingUrl(campaign,lang);const featured=score(campaign)>=65;const logo=brandLogo(campaign);return <a ref={ref} className="cleanProduct" href={href} onClick={()=>trackEvent('select_offer',{offer_id:campaign.id,partner:campaign.advertiser,offer_name:campaign.name,network:campaign.network||'impact',language:lang,destination:'landing_page'})} style={{textDecoration:'none',color:'inherit',overflow:'hidden'}}><div className="cleanProductImage" style={{minHeight:190,position:'relative',overflow:'hidden',background:'#f8faf9',padding:18,color:'#111827',borderBottom:'1px solid #e7ece8'}}><span className="cleanBadge">{featured?(lang==='pt'?'Destaque':'Featured'):(campaign.type||'Partner')}</span><div style={{height:154,width:'100%',display:'flex',flexDirection:'column',justifyContent:'space-between'}}><div style={{display:'flex',justifyContent:'space-between',alignItems:'flex-start'}}><span style={{fontSize:10,fontWeight:900,letterSpacing:1.3,color:'#64748b'}}>{categoryLabel(categoryOf(campaign),lang).toUpperCase()}</span><div style={{width:68,height:68,borderRadius:16,display:'grid',placeItems:'center',background:'#fff',border:'1px solid #dfe6e1',boxShadow:'0 5px 18px rgba(15,23,42,.07)'}}>{logo?<img src={logo} alt={`${campaign.advertiser} logo`} style={{width:48,height:48,objectFit:'contain'}}/>:<strong style={{fontSize:25,color:'#159447'}}>{campaign.advertiser.slice(0,1).toUpperCase()}</strong>}</div></div><div><div style={{fontSize:10,fontWeight:900,letterSpacing:1.2,color:'#159447'}}>VANTACART • {(campaign.network||'impact').toUpperCase()}</div><strong style={{display:'block',fontSize:20,lineHeight:1.12,marginTop:6,overflowWrap:'anywhere'}}>{campaign.name}</strong></div></div></div><div className="cleanProductBody"><div style={{fontSize:11,fontWeight:800,color:'#166534',marginBottom:5}}>{campaign.advertiser}</div><div className="cleanProductTitle" style={{display:'block',minHeight:'auto',overflow:'visible',fontSize:13,fontWeight:800}}>{campaign.name}</div><div style={{fontSize:11,fontWeight:800,color:'#334155',marginTop:8,padding:'8px 9px',background:'#f4f8f5',borderRadius:6}}>✓ {quickBenefit(campaign,lang)}</div><p style={{fontSize:11,lineHeight:1.45,color:'#64748b',margin:'8px 0 10px',display:'-webkit-box',WebkitLineClamp:2,WebkitBoxOrient:'vertical',overflow:'hidden'}}>{shortDescription(campaign,lang)}</p><div className="deliveryHint"><CheckCircle2 size={13}/>{lang==='pt'?'Programa afiliado ativo':'Active affiliate program'}</div><div style={{display:'flex',alignItems:'center',gap:7,marginTop:11,fontWeight:800,color:'#166534',fontSize:12}}>{lang==='pt'?'Ver oferta e condições':'View offer & details'} <ArrowRight size={14}/></div></div></a>}
export default function ImpactCampaignGrid({lang,initialQuery='',initialCategory='all'}:{lang:Lang;initialQuery?:string;initialCategory?:string}){const[campaigns,setCampaigns]=useState<Campaign[]>([]);const[loading,setLoading]=useState(true);const[query,setQuery]=useState(initialQuery);const[category,setCategory]=useState<Category>((['all','ai','business','home','creators','productivity','technology','gaming'].includes(initialCategory)?initialCategory:'all') as Category);useEffect(()=>{let cancelled=false;try{const cached=localStorage.getItem(CACHE_KEY);if(cached)setCampaigns(JSON.parse(cached));}catch{}Promise.allSettled([fetch('/api/impact/campaigns',{cache:'no-store'}).then(r=>r.json()),fetch('/api/hotmart/offers',{cache:'no-store'}).then(r=>r.json())]).then(results=>{if(cancelled)return;const impact=results[0].status==='fulfilled'&&results[0].value?.ok?results[0].value.campaigns.filter((c:Campaign)=>c.status==='Active'&&c.trackingLink).map((c:Campaign)=>({...c,network:'impact' as const})):[];const hotmart=results[1].status==='fulfilled'&&results[1].value?.ok?results[1].value.offers.filter((o:any)=>o.active&&o.hotlink).map((o:any)=>({id:`hotmart-${o.id}`,name:o.name,advertiser:o.producer||o.name,description:o.description,trackingLink:o.hotlink,status:'Active',type:'Hotmart',network:'hotmart' as const,category:o.category})):[];const merged=[...impact,...hotmart];setCampaigns(merged);try{localStorage.setItem(CACHE_KEY,JSON.stringify(merged));}catch{}}).finally(()=>{if(!cancelled)setLoading(false)});return()=>{cancelled=true}},[]);const ranked=useMemo(()=>[...campaigns].sort((a,b)=>score(b)-score(a)||a.name.localeCompare(b.name)),[campaigns]);const filtered=useMemo(()=>{const q=query.trim().toLowerCase();return ranked.filter(c=>(category==='all'||categoryOf(c)===category)&&(!q||`${c.name} ${c.advertiser} ${c.description||''}`.toLowerCase().includes(q)))},[ranked,query,category]);return <><div style={{display:'grid',gridTemplateColumns:'minmax(220px,1fr) auto',gap:10,margin:'0 0 14px'}}><label style={{height:42,display:'flex',alignItems:'center',gap:9,background:'#fff',border:'1px solid #dce4de',borderRadius:8,padding:'0 12px'}}><Search size={16}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder={lang==='pt'?'Buscar dentro das ofertas...':'Search within offers...'} style={{border:0,outline:0,width:'100%',fontSize:13,background:'transparent'}}/></label><div style={{display:'flex',alignItems:'center',gap:7,color:'#64748b',fontSize:11}}><SlidersHorizontal size={15}/>{filtered.length} {lang==='pt'?'resultados':'results'}</div></div><div style={{display:'flex',gap:7,overflowX:'auto',paddingBottom:10}}>{(['all','ai','business','gaming','home','creators','productivity','technology'] as Category[]).map(cat=><button key={cat} onClick={()=>setCategory(cat)} style={{whiteSpace:'nowrap',border:`1px solid ${category===cat?'#159447':'#dfe6e1'}`,background:category===cat?'#eaf8ef':'#fff',borderRadius:999,padding:'8px 11px',fontSize:11,fontWeight:800}}>{categoryLabel(cat,lang)}</button>)}</div>{loading&&!campaigns.length?<div style={{padding:30}}>Carregando ofertas...</div>:filtered.length?<div className="cleanProducts">{filtered.map((c,i)=><OfferCard key={`${c.network}-${c.id}`} campaign={c} index={i} lang={lang}/>)}</div>:<div style={{padding:30,background:'#fff'}}>{lang==='pt'?'Nenhuma oferta encontrada.':'No offers found.'}</div>}<div style={{display:'flex',alignItems:'center',gap:6,marginTop:14,fontSize:11,color:'#64748b'}}><ExternalLink size={12}/>{lang==='pt'?'Impact + Hotmart em uma única vitrine. A contratação é finalizada no site oficial do parceiro.':'Impact + Hotmart in one storefront. Purchase is completed on the partner official site.'}</div></>}
