import Link from 'next/link';
import {listOrders} from '../../../lib/orders';
export const dynamic='force-dynamic';
function money(v:any,c='usd'){return new Intl.NumberFormat('en-US',{style:'currency',currency:String(c||'usd').toUpperCase()}).format(Number(v||0));}
function fulfillmentClass(s:string){return ['DELIVERED','SHIPPED','SUBMITTED_TO_CJ'].includes(s)?'ok':['FULFILLMENT_FAILED','CANCELLED'].includes(s)?'warn':'';}
export default async function OrdersPage(){const orders=await listOrders(100);return <div className="adminWrap"><aside className="side"><div className="brand">Vanta<span>Cart</span></div><div className="menu"><Link href="/admin">Overview</Link><Link href="/admin/discover">Discover Products</Link><Link className="active" href="/admin/orders">Orders</Link></div></aside><main className="main"><div className="topline"><div><div className="eyebrow" style={{color:'#1f5147'}}>OPERATIONS</div><h1>Orders</h1><div className="meta">Stripe payments, CJ fulfillment and shipment tracking in one place.</div></div><span className="pill ok">{orders.filter(o=>o.payment_status==='PAID').length} PAID</span></div><section className="panel" style={{marginTop:18}}>{orders.length===0?<p className="meta">No orders yet.</p>:<table className="table"><thead><tr><th>Order</th><th>Customer</th><th>Items</th><th>Total</th><th>Payment</th><th>CJ / Fulfillment</th><th>Tracking</th><th>Destination</th><th>Created</th></tr></thead><tbody>{orders.map(o=><tr key={o.public_id}><td><b>{o.public_id}</b>{o.cj_order_id&&<div className="meta">CJ: {o.cj_order_id}</div>}</td><td>{o.customer_email||'—'}</td><td>{Array.isArray(o.items)?o.items.map((i:any)=><div key={`${i.cj_variant_id}-${i.product_name}`}><b>{i.quantity}×</b> {i.product_name}{i.variant_name?` — ${i.variant_name}`:''}</div>):'—'}</td><td>{money(o.total_amount,o.currency)}</td><td><span className={`pill ${o.payment_status==='PAID'?'ok':'warn'}`}>{o.payment_status}</span></td><td><span className={`pill ${fulfillmentClass(o.fulfillment_status)}`}>{o.fulfillment_status}</span>{o.cj_status&&<div className="meta">CJ: {o.cj_status}{o.cj_sub_status?` / ${o.cj_sub_status}`:''}</div>}{o.fulfillment_error&&<div className="meta">{o.fulfillment_error}</div>}</td><td>{o.tracking_number?<><b>{o.tracking_number}</b>{o.tracking_url&&<div><a href={o.tracking_url} target="_blank" rel="noreferrer">Track shipment</a></div>}</>:<span className="meta">Waiting for tracking</span>}</td><td>{[o.destination_country,o.destination_postal_code].filter(Boolean).join(' / ')||'—'}<div className="meta">{o.shipping_method||''}</div></td><td>{o.created_at?new Date(o.created_at).toLocaleString('pt-BR'):'—'}</td></tr>)}</tbody></table>}</section></main></div>;}
