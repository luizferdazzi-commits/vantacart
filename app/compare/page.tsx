import Link from 'next/link';import '../editorial.css';import {entriesByKind,Lang} from '../../lib/editorial';
export const metadata={title:'Comparativos de ferramentas digitais',description:'Comparativos para decidir entre abordagens, ferramentas e fluxos digitais.'};
export default async function Page({searchParams}:{searchParams:Promise<{lang?:string}>}){const sp=await searchParams;const lang:Lang=sp.lang==='en'?'en':'pt';const pt=lang==='pt';return <main className="editorialIndex"><nav className="editorialNav"><Link href={`/?lang=${lang}`}>← VantaCart</Link></nav><div className="editorialWrap"><h1>{pt?'Comparativos':'Comparisons'}</h1><p>{pt?'Compare alternativas pela necessidade, não pela comissão.':'Compare alternatives by need, not by commission.'}</p><div className="editorialIndexGrid">{entriesByKind('compare').map(e=><Link className="editorialIndexCard" href={`/compare/${e.slug}?lang=${lang}`} key={e.slug}><span>{e.category}</span><h2>{e.title[lang]}</h2><p>{e.description[lang]}</p></Link>)}</div></div></main>}
